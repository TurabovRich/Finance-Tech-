# Flutter platform folders

Run from `mobile/` after installing Flutter:

```bash
flutter create . --project-name clarity --org com.clarity.learn
```

This generates `android/` and `ios/` while keeping the existing `lib/` structure.
